#include <iostream>

template <typename T> // 템플릿 선언

class Widget; // 클래스 선언

std::size_t numDigits(int number); // 함수 선언
/*
Signature
매개변수 목록, 반환 타입

numDigits의 Signature는 std::size_t (int)
*/

extern int x; // 객체 선언

int	main(void)
{

	return 0;
}
